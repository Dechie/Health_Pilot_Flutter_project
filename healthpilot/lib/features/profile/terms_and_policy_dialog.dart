// Deprecated path shim: terms/policy dialog still lives under onboarding for now.
// Keep this export so Settings can depend on `features/profile/` and we can move
// the implementation later without editing every import.
export 'package:healthpilot/features/onboarding/terms_dialogBox.dart' show Policy;

