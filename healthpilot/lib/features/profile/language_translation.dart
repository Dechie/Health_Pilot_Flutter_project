// Deprecated path shim: language UI still lives under onboarding for now.
// Keep this export so Settings can depend on `features/profile/` while the actual
// implementation is migrated later (Branch F: language settings).
export 'package:healthpilot/features/onboarding/language_translation.dart'
    show LanguageTranslation;

